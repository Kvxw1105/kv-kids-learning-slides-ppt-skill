# Repository Setup

Recommended repository name:

```text
kv-kids-learning-slides
```

Recommended initial visibility:

```text
Private
```

Recommended description:

```text
Commercial-grade child-friendly PPT/PPTX generation Skill with editable decks, bilingual scripts, storybook art direction and visual QA.
```

After the private repository is created, push the prepared `main` branch. Enable GitHub Pages from `docs/` only when a public-facing repository or an approved public release strategy is ready.
